# LexVeritas 快速开始

> 5 分钟快速搭建 LexVeritas 开发环境

---

## 📋 前置要求

- **Go**: 1.21+
- **Node.js**: 18+
- **Docker**: 20.10+
- **Docker Compose**: 2.0+

---

## 🚀 快速启动

### 1. 克隆仓库

```bash
git clone https://github.com/your-org/LexVeritas.git
cd LexVeritas
```

### 2. 启动依赖服务

```bash
# 启动 Milvus、etcd、MinIO
docker-compose up -d
```

### 3. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 填入必要配置
```

### 4. 运行后端

```bash
cd backend
go mod download
go run cmd/server/main.go
```

### 5. 运行前端

```bash
cd frontend
npm install
npm start
```

访问 http://localhost:3000 查看效果。

---

## 🏗️ 架构概览

LexVeritas 采用三层架构:

```
┌─────────────────────────────────┐
│   前端 (React)                  │
│   - Chat 界面                   │
│   - 引用展示                    │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   后端 (Go + Eino)              │
│   - RAG 编排                    │
│   - Merkle 验证                 │
└─────────────────────────────────┘
              ↓
┌──────────────┬──────────────────┐
│  Milvus      │  Polygon Amoy    │
│  (向量检索)  │  (存证验证)      │
└──────────────┴──────────────────┘
```

### 核心技术栈

| 层级       | 技术                          | 说明             |
| :--------- | :---------------------------- | :--------------- |
| 前端       | React + TypeScript            | 用户交互界面     |
| 后端       | Go + Gin + Eino               | RAG 编排引擎     |
| 向量数据库 | Milvus                        | 语义检索         |
| 区块链     | Polygon Amoy                  | Merkle Root 存证 |
| 嵌入模型   | OpenAI text-embedding-3-small | 1536 维向量      |
| LLM        | OpenAI GPT-4                  | 答案生成         |

---

## 📂 项目结构

```
LexVeritas/
├── backend/                 # Go 后端
│   ├── cmd/server/         # 入口文件
│   └── internal/           # 业务逻辑
├── frontend/               # React 前端
├── blockchain/             # 智能合约
│   └── contracts/
├── data-pipeline/          # Python 数据处理
│   └── scripts/
├── docs/                   # 文档
│   └── setup/             # 本系列指南
└── docker-compose.yml      # 服务编排
```

---

## 🎯 核心特性

### 1. 结构化感知分块

法律文档按**法条**为原子单位分块,保持 100%完整性。

### 2. 三层检索架构

- **第一层**: 语义检索 (Top-K 向量相似度)
- **第二层**: 引用扩展 (自动加载引用法条)
- **第三层**: 上下文补全 (补充章节背景)

### 3. Merkle 验证

每个检索结果都经过区块链验证,确保数据未被篡改。

---

## 📚 下一步

根据您的角色选择对应指南:

| 角色          | 指南                                        | 内容            |
| :------------ | :------------------------------------------ | :-------------- |
| 合约开发者    | [区块链层实现](./02_blockchain_layer.md)    | 智能合约部署    |
| 数据工程师    | [向量数据库配置](./03_vector_database.md)   | Milvus Schema   |
| 数据科学家    | [数据处理管道](./04_data_pipeline.md)       | 分块与向量化    |
| Go 开发者     | [后端开发指南](./05_backend_development.md) | Eino Graph 编排 |
| DevOps 工程师 | [部署与运维](./06_deployment.md)            | Docker 部署     |

---

## ❓ 常见问题

**Q: Milvus 启动失败?**

```bash
docker-compose logs milvus
# 检查端口占用: lsof -i :19530
```

**Q: 后端连接区块链失败?**

```bash
# 检查 Polygon Amoy RPC 可用性
curl https://rpc-amoy.polygon.technology
```

**Q: 向量化速度慢?**
使用批量处理:

```python
embeddings = client.embeddings.create(
    model="text-embedding-3-small",
    input=[chunk1, chunk2, ...]  # 批量
)
```

---

## 🔗 相关文档

- [法律分块策略设计](../legal_chunking_strategy.md)
- [系统设计报告](../LexVeritas：基于区块链可信存证与检索增强生成（vRAG）的法律智能问答系统深度设计报告.md)
